# AutoBE Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
PyInstaller>=6.0.0
json5>=0.9.0
requests>=2.31.0
Pillow>=9.0.0

# Windows-specific (if on Windows)
wmi>=1.5.1; sys_platform == 'win32'

# Optional: background music (place .ogg or .mp3 in a "music" folder as background.ogg / lofi.ogg etc.)
pygame>=2.5.0

# Optional: Discord Rich Presence (shows "Using AutoBE" in Discord when app is open)
pypresence>=4.2.0

# Optional: For obfuscation (uncomment if using)
# pyarmor>=8.0.0
